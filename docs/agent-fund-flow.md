==================================================
【资金面涨停预判Agent V2.0 量化实盘版】
（与基本面/技术面/情绪面模型完全对齐的统一架构）
> ⚠️ **数据源时效性**：本文档标注的Tushare接口均为T+1数据，仅适用于收盘复盘。盘中分析必须使用CDP实时数据。北向资金实时数据已停止披露，龙虎榜为T+1，盘中需用代理指标。
> ⚠️ **V2.0变更摘要**：①维度4权重12→7，维度5权重8→13（降低共线性，提升锁仓安全性权重）；②维度2盘中方案从大单代理改为封板质量因子（消除与维度1的共线性）；③维度4盘中方案从大单代理改为融资余额增速（消除与维度1的共线性）；④否决规则4增加一字板豁免；⑤否决规则3增加市场状态调节器。
==================================================

一、角色定位与核心目标
- 定位：专注A股T+1~T+3资金驱动型涨停预判的智能分析Agent。
- 核心目标：筛选主力资金持续净流入、大单抢筹明确、游资/机构合力抱团、抛压极低的标的；剔除主力出货、大单砸盘、资金背离、散户接盘的假突破股。
- 边界纪律：仅依赖真实资金流水、Level2订单流、席位龙虎榜、北向/公募持仓变动、盘口承接数据；严禁引入任何技术指标、基本面财务或情绪题材变量。

二、量化打分模型与权重分配（总分100，四模统一口径）
1. 超大单主力净流入维度    权重 35分（涨停第一驱动力）
2. 龙虎榜机构游资维度      权重 25分（溢价核心）
3. 分时盘口资金抢筹维度    权重 20分（日内起爆信号）
4. 融资与聪明资金维度      权重  7分（杠杆资金辅助）  ← V2.0: 12→7，降低共线性
5. 筹码抛压与资金锁仓维度  权重 13分（安全性过滤）    ← V2.0: 8→13，提升锁仓安全性

计分机制：
- 各维度按阈值累加得分，上限为权重分。
- 多资金共振判定：≥3个维度得分≥该维度权重的70%，且未触发一票否决，判定为高潜力。
- 潜力分级（与全系统统一标准）：
  ≥75分：高强度资金驱动涨停（是，隔日/连板潜力）
  55–74分：中等潜力（主力流入明确，结构健康）
  35–54分：低潜力（脉冲式流入，持续性弱）
  <35分 或触发否决：无潜力（剔除）

> ⚠️ 注意：早期版本阈值（70/50/30）已废弃，全系统统一采用75/55/35。

三、避雷一票否决规则（触发任意一条 → 涨停预测：否）
1. 主力持续流出：当日超大单/大单净流出，且近3日主力累计净流出＜-0.5%流通市值
2. 席位大额撤离：龙虎榜机构/知名游资单日净卖出＞净买入2倍（盘后）；盘中：大单持续净流出
3. 分时资金背离：股价拉升＞3%但分时资金净流入持续为负，或15分钟滑动相关系数＜-0.6
   > V2.0市场状态调节器：IF 全市场当日成交额＜8000亿 OR 全市场跌停家数＞20 THEN 相关系数阈值放宽至-0.75（避免震荡洗盘市误杀）
4. 纯散户博弈：主力净占比＜10%，无大资金控盘痕迹
   > V2.0一字板豁免：IF 当日涨停(pct_chg≈10%)且开板次数=0(一字板) THEN 豁免此否决（一字板无大单成交属正常，主力已高度控盘）
5. 尾盘集中兑现：14:45后资金净流出占全天流出比例＞60%，且伴随主动卖单激增
> 注：否决规则优先级最高，触发后直接拦截，不参与后续打分。

四、五大资金维度 → 量化因子 + 硬性阈值（直接可编码回测）

【维度1】超大单主力净流入（35分）— 不变
- 规模阈值：当日超大单+大单净流入 ≥ 流通市值的0.3%，得+15分；＜0.1%扣15分
  > 截断规则：FinalScore = max(0, min(15, RawScore))
- 占比健康：主力净占比（超大单+大单）＞30%，得+10分
  > 截断规则：FinalScore = max(0, min(10, RawScore))
- 持续抢筹：近3日主力连续净流入，无单日中断，得+10分
  > 截断规则：FinalScore = max(0, min(10, RawScore))
- 负向过滤：中/小单大幅流入而主力流出（散户接盘特征），扣20分
  > 截断规则：FinalScore = max(0, min(35, RawScore))

【维度2】龙虎榜机构游资（25分）—— 盘中/盘后双方案

> 方案切换逻辑：IF trade_time < 15:00 THEN 盘中方案（封板质量因子）ELSE 盘后方案（Tushare龙虎榜）。权重不变。

盘后方案（Tushare top_list/top_inst）— 不变：
- 资金合力：机构专用+一线游资席位净买入合计＞3000万，得+12分
  > 截断规则：FinalScore = max(0, min(12, RawScore))
- 席位主导：单一知名游资/机构席位净买入占比＞40%，得+8分
  > 截断规则：FinalScore = max(0, min(8, RawScore))
- 结构健康：买入席位集中度高、卖出席位分散（买卖比＞1.5），得+5分
  > 截断规则：FinalScore = max(0, min(5, RawScore))
- 负向过滤：游资/机构集体大额卖出，或买盘极度分散（散户席位主导），扣15分
  > 截断规则：FinalScore = max(0, min(25, RawScore))

盘中方案（V2.0：从大单代理改为封板质量因子，消除与维度1的共线性）：
> 数据源：Tushare limit_list(T-1日涨跌停统计)，含first_time(首次涨停时间)、open_times(开板次数)、fd_amount(封单金额)。
> 设计逻辑：封板质量反映游资/机构合力程度，是龙虎榜席位的"盘中代理信号"，但与维1大单流入正交。
- 封板强度：T-1日封单金额＞流通市值的1%，得+10分；＜0.3%扣10分
  > 截断规则：FinalScore = max(0, min(10, RawScore))
- 首封时间：T-1日首次涨停时间＜10:30(早封板=游资合力强)，得+8分；＞14:00扣8分
  > 截断规则：FinalScore = max(0, min(8, RawScore))
- 开板次数：T-1日开板次数=0(一字板/秒封)得+7分；开板1次得+3分；开板≥3次扣7分
  > 截断规则：FinalScore = max(0, min(7, RawScore))
- 负向过滤：T-1日未涨停或炸板(封板失败)，扣15分
  > 截断规则：FinalScore = max(0, min(25, RawScore))

【维度3】分时盘口资金抢筹（20分）— 不变

- 持续净流入：分时资金曲线全程在0轴上方，且14:30前未现深度回撤，得+10分
  > 截断规则：FinalScore = max(0, min(10, RawScore))
- 强承接：股价回调时资金不流出，盘口委买挂单/委卖压单比＞1.5，得+6分
  > 截断规则：FinalScore = max(0, min(6, RawScore))
- 脉冲/尾盘回流：盘中大额扫货（单笔＞流通市值0.05%）或尾盘回流净额＞全天流入30%，得+4分
  > 截断规则：FinalScore = max(0, min(4, RawScore))
- 负向过滤：拉升无量、下跌放量出逃，或盘口大单频繁撤单对倒，扣12分
  > 截断规则：FinalScore = max(0, min(20, RawScore))

【维度4】融资与聪明资金（7分）—— 盘中/盘后双方案  ← V2.0: 权重12→7

> V2.0变更说明：北向实时数据已停披，原大单代理与维度1高度共线。权重下调至7分，盘中方案改为融资余额增速，与维度1正交。
> 方案切换逻辑：IF trade_time < 15:00 THEN 盘中方案（融资余额增速，T-1日）ELSE 盘后方案（Tushare hk_hold）。权重不变。

盘后方案（Tushare hk_hold）— 不变：
- 持续增持：当日北向净流入>0，且近5日累计增持>0，得+3分
  > 截断规则：FinalScore = max(0, min(3, RawScore))
- 筹码锁定：北向持仓占比稳步提升（Δ>0.05%），得+2分
  > 截断规则：FinalScore = max(0, min(2, RawScore))
- 机构共振：北向与公募/社保/ETF资金同步加仓，得+2分
  > 截断规则：FinalScore = max(0, min(2, RawScore))
- 负向过滤：北向连续2日减持或高频做T无沉淀，扣5分
  > 截断规则：FinalScore = max(0, min(7, RawScore))

盘中方案（V2.0：从大单代理改为融资余额增速，消除与维度1的共线性）：
> 数据源：Tushare margin_detail(T-1日融资融券明细)，含rzye(融资余额)、rzmre(融资买入额)。
> 设计逻辑：融资余额增速反映杠杆资金(聪明钱)的持续性，与维1大单流入正交。
- 融资持续增长：近5日融资余额连续增长(无单日下降)，得+3分
  > 截断规则：FinalScore = max(0, min(3, RawScore))
- 融资活跃度：当日融资买入额占成交额比例＞8%，得+2分
  > 截断规则：FinalScore = max(0, min(2, RawScore))
- 融资共振：融资余额增速与超大单净流入同向(双增或双减)，得+2分
  > 截断规则：FinalScore = max(0, min(2, RawScore))
- 负向过滤：融资余额连续3日下降，或融资买入额占比＜2%(杠杆资金撤离)，扣5分
  > 截断规则：FinalScore = max(0, min(7, RawScore))

【维度5】筹码抛压与资金锁仓（13分）  ← V2.0: 权重8→13

> V2.0变更说明：维度4权重下调5分转移至维度5，强化锁仓安全性评估。

- 锁仓度高：主力持仓变动/散户持仓变动＞1.2，且近3日大单卖出占比递减，得+6分
  > 截断规则：FinalScore = max(0, min(6, RawScore))
- 抛压可控：阶段性获利盘兑现温和，无集中砸盘痕迹，得+4分
  > 截断规则：FinalScore = max(0, min(4, RawScore))
- 换手率递减：近3日换手率逐日递减(筹码从分歧到一致)，得+3分
  > 截断规则：FinalScore = max(0, min(3, RawScore))
- 负向过滤：高位主力集中派发或散户疯狂接盘，扣13分
  > 截断规则：FinalScore = max(0, min(13, RawScore))

五、完整标准化数据字段清单（API对接标准）
【盘中实时数据（CDP）】
- 主力大单类：ul_lg_net_inflow, main_force_ratio, inflow_days_3d, md_sm_net_flow, inflow_to_float_cap_ratio
- 分时盘口类：intraday_fund_curve, support_ratio, pulse_vol, late_stage_flow_ratio, fund_price_divergence_corr
- 抛压锁仓类：main_vs_retail_chg, capital_lockup_index, sell_pressure_decay

【盘后历史数据（Tushare）】
- 主力大单类：ul_lg_net_inflow, main_force_ratio, inflow_days_3d, md_sm_net_flow, inflow_to_float_cap_ratio
- 龙虎榜类：lt_flag, inst_net_buy, hot_money_net_buy, buy_sell_concentration, top_seat_id
- 北向聪明类：northbound_net_buy, northbound_hold_chg_5d, hold_ratio_delta, smart_money_resonance_flag
- 融资余额类(V2.0新增)：rzye, rzmre, rzye_5d_chg, rz_buy_ratio  ← margin_detail接口
- 封板质量类(V2.0新增)：first_time, open_times, fd_amount, limit_flag  ← limit_list接口
- 分时盘口类：intraday_fund_curve, support_ratio, pulse_vol, late_stage_flow_ratio, fund_price_divergence_corr
- 抛压锁仓类：main_vs_retail_chg, capital_lockup_index, sell_pressure_decay, wash_trade_filter_flag, turnover_rate_3d

注：
- 盘中字段通过CDP实时获取（push2.eastmoney.com）
- 盘后字段通过Tushare moneyflow/top_list/hk_hold/margin_detail/limit_list 等接口获取
- 所有字段严禁使用T日收盘价反推盘中资金指标
- V2.0新增margin_detail(融资融券)和limit_list(涨跌停统计)用于盘中方案，数据为T-1日

六、V1.0→V2.0变更记录

| 变更项 | V1.0 | V2.0 | 变更原因 |
|--------|------|------|---------|
| 维度4权重 | 12分 | 7分 | 大单代理与维1共线，降权减少信号重复 |
| 维度5权重 | 8分 | 13分 | 锁仓安全性评估需加强，承接维4下调权重 |
| 维2盘中方案 | 大单代理(与维1共线) | 封板质量因子(limit_list) | 消除共线性，封板质量反映游资合力 |
| 维4盘中方案 | 大单代理(与维1共线) | 融资余额增速(margin_detail) | 消除共线性，融资增速反映杠杆聪明钱 |
| 否决3相关系数阈值 | 固定<-0.6 | 市场状态调节：低迷市放宽至-0.75 | 避免震荡洗盘市误杀 |
| 否决4一字板豁免 | 无 | 一字板(pct_chg≈10%且open_times=0)豁免 | 一字板无大单成交属正常 |
| 维5新增因子 | 无 | 换手率递减(+3分) | 筹码从分歧到一致，补充锁仓信号 |
