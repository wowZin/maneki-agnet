# 复盘机制 V2.4

## 概述

每日 15:00 收盘后自动触发复盘流程。V2.4 新增：
- **双排序对比**: 加权总分 vs Top3择优并排评估
- **攻击独特性因子**: 单独计算该因子的鉴别力
- **情绪熔断记录**: 熔断触发次数和冰点试探结果

## 复盘数据来源

| 数据 | 来源 | 路径 |
|------|------|------|
| 当日预测记录 | zt-team-leader | /srv/zt-team-leader/data/predictions/YYYY-MM-DD.json |
| 实际涨跌结果 | 行情API | 盘后数据接口 |
| 历史权重配置 | zt-team-leader | /srv/zt-team-leader/data/weights.json |
| 历史复盘存档 | zt-team-leader | /srv/zt-team-leader/data/reviews/YYYY-MM-DD.json |

## 复盘流程

### 1. 结果匹配

```
对当日每条推送股票(去重):
  - 查询该股票当日最终涨跌幅
  - 判定: 涨停(hit) / 未涨停(miss)
  - 判定: 胜(win) / 负(lose) — 收盘涨幅 > 扫描时涨幅 即为胜
  - 记录: weighted_score, top3_score, actual_result, hit/miss
```

### 2. 双排序命中率对比（V2.4新增）

分别计算加权总分和Top3择优的命中率：

```
加权总分排序:
  Top5:  X/5  命中率 Y%
  Top10: X/10 命中率 Y%
  
Top3择优排序:
  Top5:  X/5  命中率 Y%
  Top10: X/10 命中率 Y%
  
结论: 当前市场下 [加权/Top3] 更有效
```

### 3. 鉴别力计算

对每个子 agent:

```
鉴别力 = avg_score(命中股票) - avg_score(未命中股票)
命中率 = 命中数 / (命中数 + 未命中数) × 100%
```

鉴别力含义:
- > 0.6: 该agent评分能有效区分涨停/非涨停
- 0.3 ~ 0.6: 有一定区分能力
- < 0.3: 区分能力弱
- < 0: 评分方向反了，需要检查

#### 新增：攻击独特性因子鉴别力（V2.4）

```
攻击独特性鉴别力 = avg(攻击分_命中) - avg(攻击分_未命中)

若该因子鉴别力持续低于其他4因子 → 考虑降权或替换
```

### 4. 熔断记录（V2.4新增）

```
当日情绪状态:
  炸板率: XX%
  跌停家数: XX
  否决6熔断触发: 是/否 (XX:XX触发, XX:XX解除)
  否决7冰点试探: 是 (标的: XXX 结果: 涨停/炸板)
```

### 5. 权重调整

```
if 鉴别力 > 0.6:    delta = +5
if 0.3 < 鉴别力 <= 0.6: delta = 0
if 0 < 鉴别力 <= 0.3:   delta = -5
if 鉴别力 <= 0:         delta = -10

新权重 = clamp(当前权重 + delta, 10, 40)
归一化: 每个权重 = 新权重 / 总和 × 100
```

### 6. 案例归档

每日保存:
- 加权排序命中最高/误判最重的 Top5
- Top3排序命中最高/误判最重的 Top5
- 攻击独特性因子表现最佳的3只股票
- 供后续模型调优参考

### 7. 报告生成与推送

- 构建飞书复盘卡片（核心指标见下方模板）
- 发送到复盘专用 webhook
- 本地存档到 reviews/ 目录

## 复盘报告模板（V2.4更新）

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Maneki 复盘报告 YYYY-MM-DD
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【核心指标】
  推送总数: XX
  实际涨停: XX
  命中率:   XX%
  胜率:     XX%

【双排序对比】
  加权 Top5:  X/5  Top10:  X/10
  Top3 Top5:  X/5  Top10:  X/10
  效率: [加权/Top3] 领先

【维度鉴别力】
  基本面:    XX (有效/弱/无效)
  技术面:    XX
  资金面:    XX
  情绪面:    XX
  短线博弈:  XX
  攻击独特性: XX (V2.4新增)

【熔断状态】
  炸板率:     XX%
  跌停家数:   XX
  熔断触发:   是/否
  冰点试探:   是 (XXX) / 否

【调整建议】
  1. 攻击独特性因子鉴别力=XX, 建议[保留/降权]
  2. Top3效果[优于/劣于]加权, 建议[并行/主用一种]
```

## 周期统计

| 统计维度 | 频率 | 内容 |
|---------|:----:|------|
| 日度 | 每日15:30 | 命中率、双排序对比、鉴别力、熔断记录 |
| 周度 | 每周日 | 本周平均命中率、权重变化趋势、Top3 vs 加权周胜率 |
| 月度 | 每月末 | 月度综合评估、攻击独特性因子IC、模型调优建议 |

## 总分概念（V2.4说明）

| 指标 | 是否保留 | 说明 |
|------|:--------:|------|
| 加权总分(0-100) | ✅ | 默认排序方式，继续使用 |
| Top3择优分 | ✅ 新增 | 并行排序，捕捉极端信号 |
| 维度子分(0-100) | ✅ | 各维度独立评分保留不变 |
| 综合置信度 | ✅ | 继续推送，标注"加权/Top3" |

总分依然存在，只是排序多了个选择。两套排名同时推送到飞书供参考。
